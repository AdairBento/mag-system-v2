# =================================
# MAG System - Production Environment
# =================================

# Production Database (use connection pooling)
DATABASE_URL="postgresql://user:pass@production-host:5432/mag_system?schema=public&connection_limit=5&pool_timeout=10"

# Production API
API_PORT=3001
NODE_ENV=production

# Production JWT (use strong secret - 64+ chars)
JWT_SECRET=your-super-secure-production-secret-minimum-64-characters-long
JWT_EXPIRES_IN=7d

# Redis (production)
REDIS_HOST=your-redis-host
REDIS_PORT=6379
REDIS_PASSWORD=your-redis-password

# =================================
# SECURITY NOTES:
# - Use SSL in DATABASE_URL: ?sslmode=require
# - Rotate JWT_SECRET monthly
# - Use connection pooling (PgBouncer recommended)
# - Never commit this file with real values
# =================================
